const chai = require('chai');
const dirtyChai = require('dirty-chai');
const expect = chai.expect;
const proxyquire = require('proxyquire');

chai.use(dirtyChai);

describe('The service', function () {
  it('processes a message', function () {
    const service = pro
  });
});
